all:
  children:
    web_servers:
      hosts:
        sdc-web:
          ansible_host: localhost
          ansible_port: 2221
    db_servers:
      hosts:
        sdc-db:
          ansible_host: localhost
          ansible_port: 2222
    comms_relays:
      hosts:
        sdc-comms:
          ansible_host: localhost
          ansible_port: 2223
